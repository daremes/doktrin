export interface EventDataApi {
  actors: string;
  date: string;
  description?: string;
  director: string;
  event: string;
  isPremiere?: boolean;
  shortDescription: string;
  venue: string;
  url: string;
}
